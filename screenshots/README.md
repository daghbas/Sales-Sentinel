# Live Screenshots

The `Test, Build and Capture` GitHub Actions workflow starts the Flask application, signs in through Chromium, captures Arabic/English screens and error states, then commits the verified PNG files to this directory.
